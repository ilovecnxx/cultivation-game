apiVersion: apps/v1
kind: Deployment
metadata:
  name: combat
  namespace: cultivation-game
  labels:
    app: combat
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: combat
  template:
    metadata:
      labels:
        app: combat
    spec:
      containers:
        - name: combat
          image: cultivation-game/combat:latest
          ports:
            - containerPort: 50063
              name: grpc
          envFrom:
            - configMapRef:
                name: cultivation-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 50063
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 50063
            initialDelaySeconds: 5
            periodSeconds: 10
