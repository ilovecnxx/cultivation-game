apiVersion: v1
kind: Service
metadata:
  name: gateway
  namespace: cultivation-game
  labels:
    app: gateway
spec:
  type: ClusterIP
  selector:
    app: gateway
  ports:
    - name: ws
      port: 8080
      targetPort: 8080
    - name: http
      port: 8081
      targetPort: 8081
