apiVersion: apps/v1
kind: Deployment
metadata:
  name: world
  namespace: cultivation-game
  labels:
    app: world
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: world
  template:
    metadata:
      labels:
        app: world
    spec:
      containers:
        - name: world
          image: cultivation-game/world:latest
          ports:
            - containerPort: 50065
              name: grpc
          envFrom:
            - configMapRef:
                name: cultivation-config
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            tcpSocket:
              port: 50065
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            tcpSocket:
              port: 50065
            initialDelaySeconds: 5
            periodSeconds: 10
