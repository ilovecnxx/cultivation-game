apiVersion: v1
kind: Service
metadata:
  name: auth
  namespace: cultivation-game
  labels:
    app: auth
spec:
  type: ClusterIP
  selector:
    app: auth
  ports:
    - name: grpc
      port: 50060
      targetPort: 50060
